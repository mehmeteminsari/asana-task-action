{
  "name": "asana-task-action",
  "version": "1.0.0",
  "description": "This GitHub Action automates Asana task operations based on pull request (PR) descriptions. It is especially useful for keeping your task board in sync with development activities.",
  "main": "index.js",
  "scripts": {
    "build": "ncc build index.js -o dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.1",
    "asana": "^3.0.16"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3"
  }
}
